import PageHero from "../components/layout/PageHero";

function PrayerPoints() {
  return (
    <main className="min-h-screen bg-white text-[#1a1c1d]">
      <PageHero
        label="PrayerPoints"
        title="Prayer Points"
        description="Submit prayer requests, explore our prayer focus, and join the body in seeking God together."
      />
    </main>
  );
}

export default PrayerPoints;
